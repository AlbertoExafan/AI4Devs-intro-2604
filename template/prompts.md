CHATGPT

------------------------------------------------

<!DOCTYPE html> <html lang="en"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>Reverse String</title> </head> <body> <script src="script.js"></script> </body> </html> tengo este index.html en primer lugar, crea un formulario con un input de texto y un botón. El botón, al ser pulsado, tiene que llamar a una función Javascript llamada "reversestring" y tiene que pasarle como parámetro el contenido del input text.

------------------------------------------------

ahora haz que la función reverse string (hazlo en javascript puro, no uses jquery ni ninguna otra librería) muestre el string al revés (como lo has puesto con la función "reverse"), pero que en vez de mostrarlo por consola lo muestre en un alert.